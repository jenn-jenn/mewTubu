import { connect } from 'react-redux';
import Comments from './comment';

const msp = (state) => {
    debugger
    return {

    }
};

const mdp = (dispatch) => ({

});

export default connect(msp, mdp)(Comments);